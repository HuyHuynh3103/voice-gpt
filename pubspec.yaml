name: voice_gpt
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  chat_gpt_sdk: ^2.0.7
  chatview: ^1.2.1
  flutter:
    sdk: flutter
  flutter_config: ^2.0.0
  go_router: ^6.5.2
  http: ^0.13.5
  speech_to_text: ^6.1.1
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/countries/